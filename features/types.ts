// Typescript - shortcut to say here are the different properties and methods that this value right here has
const today = new Date();

const person = {
  age: 20
};

class Color {};

const red = new Color();
